<% if @new_restaurant.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@new_restaurant.errors.count, "error") %> prohibited
    this post from being saved:</h2>
    <ul>
      <% @new_restaurant.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <%= form_for @new_restaurant do |f| %>

    <div>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <div>
      <%= f.label :address %>
      <%= f.text_field :address %>
    </div>
    <div>
      <%= f.label :city %>
      <%= f.text_field :city %>
    </div>
    <div>
      <%= f.label :state %>
      <%= f.text_field :state %>
    </div>
    <div>
      <%= f.label :zipcode %>
      <%= f.text_field :zipcode %>
    </div>
    <div>
      <%= f.label :description %>
      <%= f.text_field :description %>
    </div>
    <div>
      <%= f.label :category %>
      <%= f.text_field :category %>
    </div>

    <div>
    <%= f.submit "Submit", class: "border radius"%>
    </div>

  <% end %>
